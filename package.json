{
  "name": "email-check-api",
  "version": "1.0.0",
  "description": "Verificação SMTP de emails via API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "smtp-client": "^0.4.0"
  }
}
